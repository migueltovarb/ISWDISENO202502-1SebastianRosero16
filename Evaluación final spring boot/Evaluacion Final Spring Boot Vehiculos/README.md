# Evaluación Final - Spring Boot Vehículos

Este directorio es una marca dentro del repositorio que identifica la entrega del proyecto "API CRUD Vehículos".

Los archivos del proyecto se mantienen en la raíz del repositorio; esta carpeta se añadió como marcador según la solicitud.

Cambios incluidos en la entrega:

- `src/main/resources/application.properties` (puerto cambiado a `8081`)
- `start-app.ps1` (script para compilar y arrancar el JAR en background)
- `README.md` (actualizado con instrucciones y nota sobre el puerto)

Para ejecutar el proyecto, revisa la `README.md` en la raíz del repositorio.
